# ยอดขายจริงจากระบบขาย → หน้า ads — Design

## แก้แผน 17 ก.ย. 2026 — อ่านของที่ระบบพี่ทัชมีอยู่แล้วตรงๆ (แทนที่ส่วนด้านล่างทั้งหมด)

สำรวจ repo `ssbgroup-platform` แล้วพบว่ามีข้อมูลที่ต้องการครบอยู่แล้ว ไม่ต้องให้ฝั่งขายเปิดฟังก์ชัน `mkt_*` ใหม่ (ลบไฟล์ `docs/handoff/*.sql` แล้ว)

| เรื่อง | ตัดสินใจ (อาร์ต 17 ก.ย.) |
|---|---|
| ทางเชื่อม | **ทาง B** — secret key `marketing_bridge` ของโปรเจกต์ขาย (พี่ทัชสร้าง ถอนได้ตัวเดียว) · สิทธิ์เต็มทั้งฐานข้อมูล จึงคุมที่คอลัมน์ที่ขอแทน |
| แหล่ง funnel + ยอดขาย | RPC `sale_dashboard_facts` (ด่าน `app_is_system` ยอมให้คีย์ระบบ) · ขอแค่ `kind, day, brand, channel, n, amount, is_new` · ตรวจซ้ำก่อนเขียนว่าไม่มีคอลัมน์เกิน |
| แหล่งเป้า | ตาราง `sale_goal` (เวอร์ชันล่าสุด + งบแอดรายแพลตฟอร์ม) → ไม่มีค่อยใช้ `sale_target` (แบบเก่า 6 ตัว) — ลำดับเดียวกับหน้าประวัติเป้าของพี่ทัช |
| ไม่ใช้ | `sale_goal_actuals` / `sale_goal_period` (ด่านสมาชิกทีมขาย คีย์ระบบได้ค่าว่าง) · `sale_dashboard()` (คืนชื่อลูกค้า/เซล) · `marketing-pulse` (นิยามเก่า ไม่มียอดขาย) |
| ยอดขาย | kind `book` = ยืนยันออเดอร์ ณ วันจ่ายงวดแรก · **ไม่หักยกเลิกและไม่รวม `sale_revenue_correction`** ให้ตรงกับแดชบอร์ดขายและที่เป้าของพี่ทัชใช้วัด (ต่างจาก P&L ได้ถ้ามีการแก้ยอด) · ยกเลิกเก็บแยก |
| ค่าแอด | ยึด Meta API ของเรา · ROAS/CPL/CAC/%Ads ต้องติดป้าย "คิดจากค่าแอด Meta" |
| คนทัก | แสดงคู่กัน: จากแอด Meta | ทีมขายกรอก (แยกช่องทาง · `inquiry_filled=false` = ทีมยังไม่กรอก ไม่ใช่ไม่มีคนทัก) |
| แบรนด์ | ระบบพี่ทัชเป็นแหล่งของ **TD · JD · TA** เท่านั้น · **JK ข้อมูลจริงอยู่อีกโปรเจกต์ Supabase ของอาร์ต** (ยังไม่ต่อ) · SF ยังไม่มีในระบบ ads |
| ความถี่ | วันละครั้งผ่าน `ads-cron` · ย้อน 14 วันทุกรอบ ทับทั้งช่องวัน×แบรนด์ · แบ่งก้อนละ 3 วัน (เพดาน PostgREST 1,000 แถว) ชนเพดานแตกเป็นรายวัน |
| ด่านกันพัง | ได้ facts ว่างทั้งช่วง = ไม่เขียน (`SALES_EMPTY_RESULT`) · คอลัมน์เกิน = ไม่เขียน (`SALES_COLUMN_LEAK`) · เป้าพังไม่ทำให้ยอดขายที่เขียนแล้วพัง แต่รายงานใน `goals.error` |

ผลเฟส 1 (16 ก.ย.): คีย์ `secret` ใช้ได้ · facts 7 วัน 681 แถว ไม่มีคอลัมน์เกิน · `sale_goal` เดือน ก.ย. ยังไม่มี · JK ไม่มีการกรอกคนทักเลย

---

## แผนเดิม 16 ก.ย. (เลิกใช้แล้ว เก็บไว้เป็นประวัติ)

วันที่ 16 ก.ย. 2026 · สถานะ: เลิกใช้ — ดูส่วนด้านบน

## ปัญหา

หน้า ads คิด ROAS จากมูลค่า purchase ที่ Meta attribute ซึ่งพิสูจน์แล้วว่าไม่ใช่ยอดจริงของธุรกิจ — 16 ก.ย. 2026 JUNTAKARN ปิดการขาย 183 ครั้ง แต่ Meta รู้มูลค่าแค่ 26,952 บาท (เฉลี่ย 147 บาท/ออเดอร์) เพราะปิดการขายทางแชท Meta จึงไม่รู้ราคา

ยอดจริงอยู่ในโปรเจกต์ขาย `fofgbjmpmywaldkfhtkx` (repo `ssbgroup-platform`) ตาราง `sale_deal`

## การตัดสินใจ

| เรื่อง | ตัดสินใจ |
|---|---|
| ทิศทาง | ดึงมาอย่างเดียว ไม่ส่งค่าแอดกลับไป (ทีมขายยังนำเข้า CSV เองตามเดิม) |
| นิยามรายได้ | `revenue_recognized_date` = วันจ่ายงวดแรก + `sale_revenue_correction.delta` ลงวันที่แก้ — ตรงกับ P&L ที่พี่ทัชดู |
| ไม่นับ | ดีลที่ `confirm_reverted` (ถอนยืนยัน) · ดีลที่ยังไม่มี `recognized_amount` |
| ประตูฝั่งขาย | ฟังก์ชัน `mkt_revenue_daily(from, to)` อ่านอย่างเดียว คืนแค่ brand · day · revenue · orders · เรียกได้เฉพาะ service_role — **พี่ทัชรีวิวและรันเอง** (ไฟล์: `docs/handoff/sales-revenue-bridge.sql`) |
| ข้อมูลที่ไหลข้ามระบบ | ตัวเลขรวมรายวันเท่านั้น ไม่มีชื่อ/เบอร์/ที่อยู่/รายการสินค้า |
| เก็บที่ไหน | `business_daily_facts` (มีอยู่แล้ว) `source='crm'` · `external_record_id = '<brand>|<day>'` |
| ความถี่ | วันละครั้งผ่าน cron ที่มีอยู่ · ดึงย้อนหลัง 14 วันทุกครั้ง เพราะยอดปรับย้อนหลังได้ |
| คีย์ | ตั้งเป็น Edge Function secret ของ marketing: `SALES_API_URL` · `SALES_API_KEY` (อาร์ตใส่เองผ่าน Dashboard → Edge Functions → Secrets) · ยังไม่ตั้ง = `sales-sync` ข้ามเงียบๆ ไม่ error |
| แบรนด์ | TD→b_td · JD→b_jk (JK Design) · TA→b_ta · JK→b_jt (JUNTAKARN) · SF ยังไม่มีในระบบ ads = ข้าม |
| การแสดงผล | ยอดขายจริงและ ROAS จริงเป็นตัวหลัก · ค่าที่ Meta attribute ยังแสดงคู่กันแต่ติดป้ายว่าคนละนิยาม · แบรนด์/วันที่ยังไม่มีข้อมูล = "—" ไม่ใช่ ฿0 |

## เฟสถัดไป (เตรียม SQL ไว้แล้ว รอพี่ทัชรีวิว)

| เฟส | ประตูฝั่งขาย | ได้อะไร | ฝั่ง marketing |
|---|---|---|---|
| 2 · funnel | `docs/handoff/sales-funnel-bridge.sql` → `mkt_funnel_daily(from,to)` | เทียบ "คนทักจาก Meta" กับ "คนทักที่เข้าระบบขาย" → เห็นจุดรั่วจริง + CPL/CAC จริง | เพิ่มคอลัมน์ใน `business_daily_facts` ที่มีอยู่แล้ว (inquiries/qualified_leads/deposits) แล้วต่อ `sales-sync` |
| 3 · เป้า | `docs/handoff/sales-goal-bridge.sql` → `mkt_goal_current(month)` | เป้าเดียวกันทั้งบริษัท เลิกตั้งเป้าซ้ำสองระบบ | หน้า ads อ่านเป้าจากระบบขายแทน `settings.ads_control.targets` |
| 4 · ระดับแคมเปญ | ต้องแก้ฝั่งขาย: เก็บ `ref` จากลิงก์ Click-to-Message ของ Meta ตอนลีดเข้า | ROAS ราย campaign/creative | ผูก ref กับ ad_id ตอนคำนวณ |

เฟส 2 มีจุดต้องตัดสินใจ: นิยาม funnel อยู่ใน `sale_dashboard_facts` ที่ล็อกด้วย `sale_is_member()` — เสนอให้ wrapper เป็น security definer ที่เรียกฟังก์ชันเดิม เพื่อให้ตัวเลขตรงกับแดชบอร์ดขาย 100% แทนการคัดลอกนิยามมาเขียนใหม่

## ไม่ทำรอบนี้

ส่งค่าแอดกลับไปที่ `mkt_spend` (ทีมขายยังนำเข้า CSV เอง ตามที่ตกลง)

## เกณฑ์ผ่าน

- เทส pure ครอบ: แปลงรหัสแบรนด์ · รวม correction เข้ากับวันเดิม · ดึงซ้ำแล้วทับไม่บวกเพิ่ม · null ≠ 0 · ช่วงวันเกิน 400 วันถูกตัด
- เทียบยอดรวมเดือน ก.ย. กับหน้าจอระบบขายของพี่ทัชแล้วตรง
- หน้า ads แสดง ROAS จริงพร้อมป้ายที่มา และแบรนด์ที่ยังไม่มีข้อมูลไม่กลายเป็น 0
